/**
 * Factory to create specific domain requester
 *
 * @param {Object} values
 * @returns {Requester}
 */
function createRequester(values) {
  return values;
}

module.exports = { createRequester };
